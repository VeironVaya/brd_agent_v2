meta {
  name: Logout
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/auth/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{logoutToken}}
}

tests {
  test("status is 204", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
