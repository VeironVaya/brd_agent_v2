meta {
  name: Post Message - General Room
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/conversations/{{conversationId}}/rooms/general/messages
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "text": "General question not tied to a specific field."
  }
}

tests {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
