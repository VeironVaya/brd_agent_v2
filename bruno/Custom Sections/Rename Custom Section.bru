meta {
  name: Rename Custom Section
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/conversations/{{conversationId}}/custom-sections/{{customSectionId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "title": "Vendor Escalation & Notification Path"
  }
}

tests {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
