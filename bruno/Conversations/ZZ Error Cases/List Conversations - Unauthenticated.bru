meta {
  name: List Conversations - Unauthenticated
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/conversations
  body: none
  auth: none
}

tests {
  test("status is 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
