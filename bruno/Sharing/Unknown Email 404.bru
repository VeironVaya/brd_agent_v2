meta {
  name: Unknown Email 404
  type: http
  seq: 16
}

post {
  url: {{baseUrl}}/api/conversations/{{sharingConversationId}}/collaborators
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "email": "bruno-nobody@example.com",
    "role": "viewer"
  }
}

tests {
  test("status is 404", function () {
    expect(res.getStatus()).to.equal(404);
  });
  test("error code is not_found", function () {
    expect(res.body.error).to.equal("not_found");
  });
}
