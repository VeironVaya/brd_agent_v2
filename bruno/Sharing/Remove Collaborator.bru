meta {
  name: Remove Collaborator
  type: http
  seq: 11
}

delete {
  url: {{baseUrl}}/api/conversations/{{sharingConversationId}}/collaborators/{{editorCollaboratorId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("status is 204", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
